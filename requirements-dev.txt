# Development and synthesis dependencies.
#
# The Lambda functions themselves have NO third-party runtime dependencies: boto3
# and urllib3 are both provided by the managed Python runtime. Nothing here is
# packaged into a deployment artefact.
#
# aws-cdk-lib must stay compatible with the CDK CLI pinned in package.json. The two
# version independently, and a mismatch fails with an opaque cloud-assembly schema
# error rather than anything that names the cause.
aws-cdk-lib>=2.266.0,<3.0.0
constructs>=10.0.0,<11.0.0

pyyaml>=6.0

# Present so tests can import the handlers, which reference them. At runtime the
# Lambda uses the versions the runtime ships.
boto3>=1.34
urllib3>=2.0

pytest>=8.0
pytest-cov>=5.0
ruff>=0.6
